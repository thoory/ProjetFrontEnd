language: node_js
node_js:
  - "node"
  
before_script:
  - npm install -g @angular/cli
script:
  - ng build --env=prod
  
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master